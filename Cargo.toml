[package]
name = "falcon-jsonapi"
version = "1.0.0"
edition = "2024"
description = "Shared JSON:API protocol types — documents, resources, relationships and path builders"
license = "MIT"
repository = "https://github.com/captomatic/falcon-jsonapi"
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
